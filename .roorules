# 缠论 Bot - Roo Code 专属项目规则

## 1. 架构速览
- 项目目标：基于缠论的自动扫描与交易系统。
- 关键模块：扫描获取数据、视觉形态识别（顶底、笔、中枢）、策略下单。

## 2. 工作流指令（与外部脚本联动）
- **[任务获取]**：如果我让你执行任务，请先去阅读当前目录下的 `TASK_INBOX.md` 或直接听从我的指令。
- **[交卷机制]**：当且仅当你完成了所有的代码修改、Bug 修复，并且认为可以向我交付成果时，**你必须执行终端命令 `touch READY_FOR_REVIEW.txt`** 来创建一个空文件。这是触发我后台 Git 自动化脚本推送到手机端审查的唯一信号。

## 3. 开发底线
- 修改代码时，不要大面积删除你看不懂的代码，如果有疑惑，先通过 `ask` 提问。
- 保证模块解耦，视觉判断逻辑绝对不能污染下单逻辑。